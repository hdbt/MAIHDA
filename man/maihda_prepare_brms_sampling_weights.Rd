% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_weights.R
\name{maihda_prepare_brms_sampling_weights}
\alias{maihda_prepare_brms_sampling_weights}
\title{Prepare data and formula for a sampling-weighted brms fit}
\usage{
maihda_prepare_brms_sampling_weights(data, formula, sampling_weights)
}
\arguments{
\item{data}{The model data.}

\item{formula}{The model formula.}

\item{sampling_weights}{Name of the sampling-weight column.}
}
\value{
A list with \code{data} (weights column \code{.maihda_sw} added) and
  \code{formula} (rewritten).
}
\description{
Drops rows with missing or non-positive sampling weights (with a warning),
normalizes the remaining weights to mean 1 -- likelihood weights scale the
effective sample size, so unnormalized expansion weights (summing to the
population) would massively overstate the information in the data -- and
rewrites the formula with a \code{weights()} addition term.
}
\keyword{internal}
