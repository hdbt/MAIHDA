% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_weights.R
\name{maihda_brms_weights_formula}
\alias{maihda_brms_weights_formula}
\title{Inject sampling weights into a brms formula}
\usage{
maihda_brms_weights_formula(formula, wcol)
}
\arguments{
\item{formula}{The model formula.}

\item{wcol}{Name of the (normalized) weight column.}
}
\value{
The rewritten formula (same environment).
}
\description{
Rewrites \code{y ~ ...} as \code{y | weights(w) ~ ...}. An existing addition
term (e.g. an aggregated-binomial \code{y | trials(n)}) is extended with
\code{+ weights(w)}; a formula that already carries a \code{weights()} addition
term is rejected (the two weight specifications would conflict).
}
\keyword{internal}
