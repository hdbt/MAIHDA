% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_weights.R
\name{maihda_fit_wemix}
\alias{maihda_fit_wemix}
\title{Fit a design-weighted MAIHDA model via WeMix}
\usage{
maihda_fit_wemix(formula, data, family, sampling_weights, dot_vals)
}
\arguments{
\item{formula}{The resolved model formula (with \code{(1 | stratum)}).}

\item{data}{The data (after strata creation / response recoding).}

\item{family}{The resolved family object (gaussian-identity or binomial-logit).}

\item{sampling_weights}{Name of the level-1 sampling-weight column.}

\item{dot_vals}{Named list of evaluated \code{...} arguments forwarded to
\code{WeMix::mix()} (e.g. \code{nQuad}, \code{verbose}, \code{fast}).}
}
\value{
A list with \code{model} (the \code{WeMixResults}) and \code{data}
  (the analytic data frame actually fitted, including the weight columns).
}
\description{
Internal engine call for \code{fit_maihda(engine = "wemix")}. Builds the
analytic sample (complete cases on the model variables and the weight column,
positive weights only) so the stored \code{data} matches the rows WeMix fits,
attaches the constant level-2 weight column (strata are exhaustive population
cells, sampled with certainty), and calls \code{WeMix::mix()} with the
unconditional weights \code{c(level1, level2)}.
}
\keyword{internal}
