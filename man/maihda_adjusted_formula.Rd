% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_maihda.R
\name{maihda_adjusted_formula}
\alias{maihda_adjusted_formula}
\title{Build the adjusted-model formula and data for a MAIHDA decomposition}
\usage{
maihda_adjusted_formula(null_formula, strata_vars, autobin_info, data)
}
\arguments{
\item{null_formula}{The null model formula using \code{(1 | stratum)}.}

\item{strata_vars}{Character vector of stratum-defining variables.}

\item{autobin_info}{Auto-binning recipe (\code{strata_autobin_info}).}

\item{data}{The null model's data (\code{original_data}) with the dimension columns.}
}
\value{
A list with \code{formula} and \code{data}, or \code{NULL} if fewer than two
  dimensions are available.
}
\description{
Given a fitted null model's formula (in \code{(1 | stratum)} form) and its stored
strata metadata, returns the adjusted formula -- the null formula plus the additive
main effects of the stratum dimensions -- and the data carrying any reconstructed
binned factors. Returns \code{NULL} when fewer than two dimensions are available,
because there is no intersection to decompose and the single dimension's main effect
would render the stratum random intercept redundant (singular).
}
\keyword{internal}
